import { FloatsOnlyDirective } from './floats-only.directive';

describe('FloatsOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new FloatsOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
